{
  "name": "11-multi-tenant-saas",
  "version": "1.0.0",
  "description": "Multi-tenant SaaS with full Cloudflare stack",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:init": "wrangler d1 execute multi-tenant-saas --file=schema.sql",
    "db:init:local": "wrangler d1 execute multi-tenant-saas --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  }
}
