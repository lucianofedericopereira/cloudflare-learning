{
  "name": "04-link-in-bio",
  "version": "1.0.0",
  "description": "Link in Bio with Pages + D1 + Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:init": "wrangler d1 execute link-in-bio --file=schema.sql",
    "db:init:local": "wrangler d1 execute link-in-bio --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  }
}
